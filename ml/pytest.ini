# ml/pytest.ini - ML module specific configuration
[pytest]
addopts =
    -m "not gpu"  # Skip GPU tests by default
markers =
    gpu: marks tests requiring CUDA GPU
    numba: marks tests that trigger JIT compilation
